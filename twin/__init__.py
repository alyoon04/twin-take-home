"""Airtable Web API twin — implementation package.

The build is staged (see docs/PLAN.md). This package starts as a faithful
restructure of the starter (S3) and grows into a full Airtable twin. Build
behavior to docs/AIRTABLE_SPEC.md; preserve the invariants in CLAUDE.md.
"""
